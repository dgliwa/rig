# Plan 05-01: CBA Device Presets

**Phase Goal:** All three CBA pedals have rock-ready presets in rig.yaml and `rig validate` passes.
**Plan ID:** 05-01
**Depends on:** Phase 4 (complete)

---

## Objective

Add three new active preset entries to `rig.yaml`: `rock-drive` under `brothers`, `envelope-filter` under `billy-strings-wombtone`, and `chorus-shimmer` under `mood`. Each preset uses exact parameter values locked in 05-CONTEXT.md (D-01 through D-11). After all three are inserted, `rig validate` must exit 0 with no errors. These presets are the Phase 7 rock scene's direct dependencies — their IDs must be stable now.

---

## Tasks

### T1: Add Brothers rock-drive preset

**File:** `rig.yaml`

**Requirements:** BROTHERS-01

**Action:** Insert the following YAML block under `brothers.presets`, immediately after the `transparent-lift` entry (after line ending `channel_2_bypass: 0`) and before the commented-out `kot-lead-stack` block. The new entry is active (uncommented). Preset number 5 is the next available slot (slots 3 and 6 are commented-out; slot 4 = transparent-lift).

Per D-01: Ch1 BOOST gain_1: 42 and volume_1: 90 — noticeably louder and more driven than transparent-lift (gain_1: 13, volume_1: 83). Per D-02: Ch2 DIST at gain_2: 89 for Say-It-Ain't-So solo saturation. Per D-03: dip_hi_gain_2: 127 engaged for lead-sustain saturation. Per D-04: both channels active simultaneously (no channel_2_bypass field). treble_boost: 1 (off) preserves the KOT voice clean.

Dipswitch rule: 0 = off, 127 = on — never true/false/1.

```yaml
      - id: rock-drive
        name: Rock Drive
        preset_number: 5
        notes: "Dual-channel rock preset — Ch1 BOOST pushes louder than transparent-lift (Buddy Holly rhythm level); Ch2 DIST with HI GAIN 2 engaged for Say It Ain't So solo saturation. Both channels active simultaneously."
        parameters:
          gain_1: 42       # ~10 o'clock — moderate boost, noticeably driven vs transparent-lift (13)
          volume_1: 90     # ~2 o'clock — louder than transparent-lift (83); rhythm level push
          tone_1: 64       # noon — neutral
          presence_1: 0    # down — default
          gain_1_type: 2   # boost — cleanest, loudest Ch1 mode (0=boost, 1=od, 2=dist)
          gain_2: 89       # ~2 o'clock — high-gain distortion, solo territory
          volume_2: 76     # ~1 o'clock — output level with Ch2 distortion
          tone_2: 64       # noon — neutral
          presence_2: 0    # down — default
          gain_2_type: 2   # dist — hard clipping for saturation
          treble_boost: 1  # off — pure KOT voice, no Rangemaster coloring
          dip_hi_gain_2: 127  # HI GAIN 2 on — +25% gain on Ch2 for lead-sustain saturation
```

**Verification:** `rig validate` passes; `rock-drive` appears under brothers presets with `volume_1: 90` (greater than transparent-lift's `83`) and `dip_hi_gain_2: 127`.

---

### T2: Add Wombtone envelope-filter preset

**File:** `rig.yaml`

**Requirements:** WOMBTONE-01

**Action:** Insert the following YAML block under `billy-strings-wombtone.presets`, immediately after the `slow-vibe` entry and before the commented-out `lead-vibe` block. The new entry is active (uncommented). Preset number 6 is the next available slot (slots 4 and 5 are commented-out; slot 3 = slow-vibe).

Per D-05: rate: 0 — frozen phase, static cocked-wah position (not a sweep). Per D-06: feed: 100 — high resonance for pronounced vowel/wah character. Per D-07: depth: 115 — near-max, clearly audible phaser freeze. Per D-08: mix: 70 — moderate blend, vowel quality without swallowing dry signal. volume: 70 for unity. form: 64 (sine-like). ramp: 0 (no ramping). clock_division: 2 (quarter).

Dipswitch rule: 0 = off, 127 = on — never true/false/1.

```yaml
      - id: envelope-filter
        name: Envelope Filter
        preset_number: 6
        notes: "Static cocked-wah phaser — rate frozen at 0, high resonance (feed: 100) parks the phase sweep in the vowel sweet spot. Texture device in rock chain, not a performance sweep. Deep depth for obvious coloring."
        parameters:
          feed: 100        # High resonance — pronounced vowel/wah peak character
          volume: 70       # Unity
          mix: 70          # Moderate blend — filter vowel present without swallowing dry signal
          rate: 0          # Frozen — static cocked-wah position, no LFO sweep
          depth: 115       # Near-max — clearly audible phaser freeze, obvious coloring
          form: 64         # Sine-like waveform
          ramp: 0          # No ramping
          clock_division: 2  # quarter — base tempo reference
```

**Verification:** `rig validate` passes; `envelope-filter` appears under billy-strings-wombtone presets with `rate: 0` and `feed: 100`.

---

### T3: Add Mood chorus-shimmer preset

**File:** `rig.yaml`

**Requirements:** MOOD-01

**Action:** Insert the following YAML block under `mood.presets`, immediately after the `ambient-reverse-tape` entry and before the commented-out `dub-tape-delay` block. The new entry is active (uncommented). Preset number 7 is the next available slot (slots 4, 5, and 6 are commented-out; slot 3 = ambient-reverse-tape).

Per D-09: wet_channel: 1 — delay path for chorus-like shimmer. Per D-10: mix: 72 (present, audible chorus texture — Smashing Pumpkins "Today" character), clock: 60 (moderate, hi-fi with slight warmth). Per D-11: routing: 1 (out) — covers both live signal and loop output. time: 35 for short delay. wet_modify: 40 (moderate feedback). length: 50. loop_modify: 64 (neutral). ramp_speed: 0. loop: 1 (tape). dip_r_trails: 127 (TRAILS on — smooth bypass). No dip_r_latch (not needed for shimmer use case).

Dipswitch rule: 0 = off, 127 = on — never true/false/1.

```yaml
      - id: chorus-shimmer
        name: Chorus Shimmer
        preset_number: 7
        notes: "Short-delay chorus shimmer — wet_channel delay path at short time (35) creates chorus-like shimmer. Routing: out covers live signal and loop output simultaneously. Think Smashing Pumpkins 'Today' rhythm chorus texture."
        parameters:
          time: 35         # Short delay — chorus-character shimmer range
          mix: 72          # Present, audible chorus texture — not a background shimmer
          length: 50       # noon — neutral
          wet_modify: 40   # Moderate feedback — adds depth without runaway
          clock: 60        # Moderate — hi-fi with slight warmth
          loop_modify: 64  # noon — normal playback
          ramp_speed: 0    # No ramping
          wet_channel: 1   # delay — chorus-like shimmer path
          routing: 1       # out — wet applies to full output (live signal + loop)
          loop: 1          # tape — warm texture
          dip_r_trails: 127  # TRAILS on — smooth bypass fade
```

**Verification:** `rig validate` passes; `chorus-shimmer` appears under mood presets with `wet_channel: 1` and `time: 35`.

---

### T4: Validate all three presets

**Command:** Run from `/Users/derekgliwa/dev/rig/`:

```
rig validate
```

**Expected:** Exit 0, no validation errors. All three new presets (`rock-drive`, `envelope-filter`, `chorus-shimmer`) are active entries in their respective device sections.

---

## UAT Checklist

- [ ] **BROTHERS-01:** `rock-drive` preset visible under `brothers.presets` with `volume_1: 90` (exceeds transparent-lift's `volume_1: 83`) and `dip_hi_gain_2: 127`
- [ ] **WOMBTONE-01:** `envelope-filter` preset visible under `billy-strings-wombtone.presets` with `rate: 0` and `feed: 100` (>= 96)
- [ ] **MOOD-01:** `chorus-shimmer` preset visible under `mood.presets` with `wet_channel: 1` (delay path) and `time: 35`
- [ ] `rig validate` passes with all three new presets — exit 0, no errors
