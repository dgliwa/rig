---
phase: 07-rock-scene-mc6-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [rig.yaml]
autonomous: true
requirements: [SCENE-01, SCENE-02, SCENE-03]

must_haves:
  truths:
    - "`rock` scene key exists under `mc6.config.scenes` with all four device-preset pairs"
    - "MC6 Bank 1 Switch B is mapped to `scene: rock`"
    - "`rig validate` exits 0 and reports 2 scenes with all cross-references valid"
  artifacts:
    - path: "rig.yaml"
      provides: "rock scene and Switch B mapping"
      contains: "rock:"
  key_links:
    - from: "mc6.config.scenes.rock"
      to: "mc6.config.banks[0].switches.B"
      via: "scene: rock reference"
      pattern: "scene: rock"
---

<objective>
Add the `rock` scene to `rig.yaml` and wire MC6 Bank 1 Switch B to it.

Purpose: Complete the rock signal chain so all four devices (Brothers, Wombtone, HX Stomp, Mood) are activated by a single scene switch on the MC6.
Output: `rig.yaml` with `rock` scene under `mc6.config.scenes` (D-01, D-02) and Switch B assigned to `scene: rock` (D-04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-rock-scene-mc6-wiring/07-CONTEXT.md
@.planning/phases/07-rock-scene-mc6-wiring/07-RESEARCH.md
@rig.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `rock` scene to mc6.config.scenes (per D-01, D-02)</name>
  <files>rig.yaml</files>
  <action>
    In `rig.yaml`, insert the `rock` scene block immediately after the closing line of the `clean` scene (after line 309, `mood: ambient-reverse-tape`) and before line 310 (`# boogie:`). Mirror the `clean` scene structure exactly — indentation is 8 spaces for the scene key, 10 for `description:` and `presets:`, 12 for each device-preset pair.

    Insert these 7 lines (per D-01 and D-02):

        rock:
          description: "90s rock: Mesa Boogie amp, Brothers distortion, Wombtone wah, Mood chorus"
          presets:
            hx-stomp: mesa-rock
            billy-strings-wombtone: envelope-filter
            brothers: rock-drive
            mood: chorus-shimmer

    All four preset IDs are confirmed present in rig.yaml (rock-drive at line 43, envelope-filter at line 122, mesa-rock at line 176, chorus-shimmer at line 225). Device keys use the full `id` field — `billy-strings-wombtone` not `wombtone`.

    Do NOT touch any commented-out scene blocks (boogie, wacky, marshall) per D-03.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig && grep -A 7 "^        rock:" rig.yaml</automated>
  </verify>
  <done>The `rock:` key with description and all four device-preset pairs appears in rig.yaml with correct indentation, immediately before the `# boogie:` comment block.</done>
</task>

<task type="auto">
  <name>Task 2: Assign Switch B to `scene: rock` in mc6.config.banks (per D-04)</name>
  <files>rig.yaml</files>
  <action>
    In `rig.yaml`, replace the two commented-out Switch B placeholder lines (currently lines 337–338):

        # B:
        #   scene: crunch

    with the active Switch B assignment (per D-04):

            B:
              scene: rock

    Indentation mirrors Switch A exactly: 12 spaces for `B:`, 14 spaces for `scene: rock`.

    Leave all other commented-out switches (C and D) untouched per D-03.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig && grep -A 2 "^            B:" rig.yaml</automated>
  </verify>
  <done>`B:` with `scene: rock` appears in the switches block in rig.yaml, and `# C:` and `# D:` comment lines remain untouched below it.</done>
</task>

<task type="auto">
  <name>Task 3: Run `rig validate` and confirm acceptance gate (SCENE-01, SCENE-02, SCENE-03)</name>
  <files></files>
  <action>
    Run `rig validate` from `/Users/derekgliwa/dev/rig`. Do not run `rig generate mc6` — that command does not exist in the CLI.

    Expected output (both lines required):

        ✓ Derek's Rig — 5 pedals, 2 scenes
        ✓ All cross-references valid

    The scene count must be 2 (up from 1). If validation fails, diagnose the YAML indentation or device key spelling before proceeding — do not mark the task done on a failed validate.
  </action>
  <verify>
    <automated>cd /Users/derekgliwa/dev/rig && rig validate</automated>
  </verify>
  <done>`rig validate` exits 0 and outputs exactly `✓ Derek's Rig — 5 pedals, 2 scenes` and `✓ All cross-references valid`. SCENE-01, SCENE-02, and SCENE-03 are satisfied.</done>
</task>

</tasks>

<verification>
Single gate: `rig validate` from `/Users/derekgliwa/dev/rig`

Pass condition:
- Line 1: `✓ Derek's Rig — 5 pedals, 2 scenes`
- Line 2: `✓ All cross-references valid`
- Exit code: 0

If either line differs or exit code is non-zero, the phase is not complete.
</verification>

<success_criteria>
- `rock` scene exists in `mc6.config.scenes` with description per D-01 and all four device-preset pairs per D-02
- MC6 Bank 1 Switch B mapped to `scene: rock` per D-04
- All commented-out scenes and switches left as-is per D-03
- `rig validate` reports 2 scenes with no cross-reference errors (SCENE-01, SCENE-02, SCENE-03)
</success_criteria>

<output>
Create `.planning/phases/07-rock-scene-mc6-wiring/07-01-SUMMARY.md` when done.
</output>
