# Plan 06-01: HX Stomp Mesa-Rock Preset Entry

**Phase Goal:** rig.yaml contains a `mesa-rock` preset entry under the hx-stomp device and `rig validate` accepts it without errors.
**Plan ID:** 06-01
**Depends on:** Phase 4 (complete)

---

## Objective

Add a single active `mesa-rock` preset entry to `rig.yaml` under `hx-stomp.presets`, immediately after the existing `clean` entry. The entry uses preset slot 37 (per D-01), references `hlx/MesaRock.hlx` (per D-02), and is uncommented/active (per D-03). Phase 7 will reference this entry by `id: mesa-rock` — the ID must be exactly `mesa-rock`.

---

## Tasks

### T1: Add mesa-rock preset to hx-stomp in rig.yaml

**File:** `rig.yaml`

**Requirements:** HX-01

**Action:** Insert the following YAML block under `hx-stomp.presets`, immediately after the `clean` entry (after line `notes: Clean Fender tone`) and before the commented-out `marshall` block. The new entry is active (uncommented). Per D-01: `preset_number: 37` (takes the slot previously annotated to the commented-out `marshall` entry). Per D-02: `hlx_file: hlx/MesaRock.hlx` (no spaces — matches what the user will name the file in HX Edit). Per D-03: entry is active/uncommented since `rig validate` treats file-existence as advisory, not blocking.

Insert after this existing line:
```
        notes: Clean Fender tone
```

Insert this block (4-space indent under `presets:`, matching the `clean` entry):
```yaml
      - id: mesa-rock
        name: Mesa Rock
        preset_number: 37
        hlx_file: hlx/MesaRock.hlx
        notes: Mesa Boogie amp model, high-gain rock tone
```

The commented-out `marshall`, `boogie-pb`, and `doogie-pb` blocks below remain untouched.

**Verification:** `rig validate` passes with exit 0; `mesa-rock` appears under `hx-stomp.presets` with `preset_number: 37` and `hlx_file: hlx/MesaRock.hlx`.

---

### T2: Validate the new entry

**Command:** Run from `/Users/derekgliwa/dev/rig/`:

```
rig validate
```

**Expected:** Exit 0, no validation errors. The `mesa-rock` preset is an active entry under `hx-stomp.presets`.

---

## UAT Checklist

- [ ] **HX-01:** `mesa-rock` preset visible under `hx-stomp.presets` in `rig.yaml` with `preset_number: 37` and `hlx_file: hlx/MesaRock.hlx`
- [ ] `rig validate` passes — exit 0, no errors
- [ ] Entry is active (uncommented) — Phase 7 can reference `hx-stomp: mesa-rock` by ID
